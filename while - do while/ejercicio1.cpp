#include <iostream>
using namespace std;

int main() {
    int contador = 1; // Inicializamos el contador en 1

    // Mientras contador sea menor o igual a 10
    while (contador <= 10) {
        cout << contador << endl; // Imprime el valor actual del contador
        contador++; // Incrementa el contador en 1
    }

    return 0;
}